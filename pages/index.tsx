'use client'; // Tambahkan ini jika Anda berada di app/ (opsional)

import React from 'react'; // Ini tetap aman ditambahkan jika pakai TypeScript

const Home = () => {
  return (
    <div style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Welcome to TinyLlama Chatbot</h1>
      <p>Deploy sukses di Vercel!</p>
    </div>
  );
};

export default Home;
